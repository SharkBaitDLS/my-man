[package]
name = "my_man"
version = "0.4.0"
authors = ["Isaac M. Good <imgood@imgoodproductions.org>"]
edition = "2021"

[dependencies]
async-trait = "0.1"
env_logger = "*"
futures = "0.3"
log = "*"
serenity = { version = "0.10", features = ["cache", "rustls_backend", "unstable_discord_api", "voice"] }
songbird = "0.2"
tokio = { version = "1.13", features = ["macros", "rt-multi-thread"] }

[dev-dependencies]
tempfile = "*"